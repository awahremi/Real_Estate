const Register = () => {
  return <div>Register page

    work to be done;
    redo the register page and have it correctly with a background image working correctly
  </div>;
};

export default Register;
